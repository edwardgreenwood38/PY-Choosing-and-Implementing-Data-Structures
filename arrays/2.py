import numpy as np

# Define your array of numbers
array_of_numbers = np.array([1,6,3])

# Calculate the sum
array_sum = np.sum(array_of_numbers)

# Print the result
print("Sum of the array:", array_sum)
